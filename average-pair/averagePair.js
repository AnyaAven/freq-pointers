/*
given a sorted array of numbers
given a number (target average)


should be O(n)

Examples:
averagePair([1, 2, 3], 2.5);         // true
averagePair([3, 3, 6, 12, 19], 8);   // false
averagePair([1, 2, 3], 2);           // true
averagePair([], 4);                  // false
*/

function averagePair(nums, targetAvg) {
    return false;
}

export { averagePair };
